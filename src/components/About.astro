---
// Advanced About section with interactive elements
---

<section id="about" class="py-20 bg-white dark:bg-gray-900 transition-colors duration-300 sr-zoom">
	<div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
		<div class="grid lg:grid-cols-2 gap-12 items-center">
			<!-- Left Content -->
			<div class="space-y-8">
				<!-- Section Header -->
				<div class="space-y-4">
					<h2 class="text-4xl lg:text-5xl font-bold text-gray-900 dark:text-white">
						About 
						<span class="bg-gradient-to-r from-blue-600 to-purple-600 bg-clip-text text-transparent">
							Me
						</span>
					</h2>
					<div class="w-20 h-1 bg-gradient-to-r from-blue-600 to-purple-600 rounded-full"></div>
				</div>

				<!-- Description -->
				<div class="space-y-6 text-lg text-gray-600 dark:text-gray-300 leading-relaxed">
					<p class="sr-reveal" style="--sr-delay:0ms">
						I'm a passionate, aspiring <strong class="text-gray-900 dark:text-white">Software Engineer</strong> with dual international internship experience in Robotics, AI/ML, and Full-Stack Development. My work bridges advanced software engineering with intelligent automation, delivering scalable, production-grade solutions.
					</p>
					<p class="sr-reveal" style="--sr-delay:120ms">
						I specialize in modern web technologies and building autonomous robotics systems using <strong class="text-blue-600">Python</strong>, <strong class="text-green-600">ROS2</strong>, <strong class="text-orange-600">FastAPI</strong>, and <strong class="text-purple-600">Node.js</strong>, <strong class="text-green-600">Langraph</strong> and <strong class="text-blue-600">Django</strong>. I'm passionate about creating performant, accessible, and user-friendly applications that solve real-world problems.
					</p>
					<p class="sr-reveal" style="--sr-delay:240ms">
						When I’m not engineering systems, I’m exploring emerging technologies, refining my AI/robotics stack, and contributing to projects that push the boundaries of automation and intelligent systems.
					</p>
				</div>

				<!-- Stats -->
				<div class="grid grid-cols-3 gap-8 pt-8 border-t border-gray-200 dark:border-gray-700">
					<div class="text-center sr-card" style="--sr-delay:0ms" data-target="150">
						<div class="text-3xl font-bold text-blue-600 counter">0</div>
						<div class="text-sm text-gray-600 dark:text-gray-400 mt-2">Projects Completed</div>
					</div>
					<div class="text-center sr-card" style="--sr-delay:120ms" data-target="50">
						<div class="text-3xl font-bold text-green-600 counter">0</div>
						<div class="text-sm text-gray-600 dark:text-gray-400 mt-2">Happy Clients</div>
					</div>
					<div class="text-center sr-card" style="--sr-delay:240ms" data-target="5">
						<div class="text-3xl font-bold text-purple-600 counter">0</div>
						<div class="text-sm text-gray-600 dark:text-gray-400 mt-2">Years Experience</div>
					</div>
				</div>

				<!-- Skills Progress Bars -->
<div class="space-y-6">
	<h3 class="text-xl font-semibold text-gray-900 dark:text-white">Core Competencies</h3>
	<div class="space-y-4">
		<div class="skill-item sr-card" style="--sr-delay:0ms" data-skill="95">
			<div class="flex justify-between mb-2">
				<span class="text-gray-700 dark:text-gray-300 font-medium">Python / AI & ML</span>
				<span class="text-gray-500 dark:text-gray-400 skill-percentage">95%</span>
			</div>
			<div class="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2">
				<div class="bg-gradient-to-r from-yellow-400 to-orange-500 h-2 rounded-full skill-bar" style="width: 0%"></div>
			</div>
		</div>
		<div class="skill-item sr-card" style="--sr-delay:80ms" data-skill="92">
			<div class="flex justify-between mb-2">
				<span class="text-gray-700 dark:text-gray-300 font-medium">ROS2 / Robotics & SLAM</span>
				<span class="text-gray-500 dark:text-gray-400 skill-percentage">92%</span>
			</div>
			<div class="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2">
				<div class="bg-gradient-to-r from-blue-400 to-purple-500 h-2 rounded-full skill-bar" style="width: 0%"></div>
			</div>
		</div>
		<div class="skill-item sr-card" style="--sr-delay:160ms" data-skill="90">
			<div class="flex justify-between mb-2">
				<span class="text-gray-700 dark:text-gray-300 font-medium">Django / FastAPI (Full-Stack)</span>
				<span class="text-gray-500 dark:text-gray-400 skill-percentage">90%</span>
			</div>
			<div class="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2">
				<div class="bg-gradient-to-r from-green-400 to-teal-500 h-2 rounded-full skill-bar" style="width: 0%"></div>
			</div>
		</div>
		<div class="skill-item sr-card" style="--sr-delay:240ms" data-skill="88">
			<div class="flex justify-between mb-2">
				<span class="text-gray-700 dark:text-gray-300 font-medium">Computer Vision (YOLO, OpenCV)</span>
				<span class="text-gray-500 dark:text-gray-400 skill-percentage">88%</span>
			</div>
			<div class="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2">
				<div class="bg-gradient-to-r from-pink-400 to-red-500 h-2 rounded-full skill-bar" style="width: 0%"></div>
			</div>
		</div>
		<div class="skill-item sr-card" style="--sr-delay:320ms" data-skill="85">
			<div class="flex justify-between mb-2">
				<span class="text-gray-700 dark:text-gray-300 font-medium">Database Design & Cloud (MySQL, PostgreSQL, AWS)</span>
				<span class="text-gray-500 dark:text-gray-400 skill-percentage">85%</span>
			</div>
			<div class="w-full bg-gray-200 dark:bg-gray-700 rounded-full h-2">
				<div class="bg-gradient-to-r from-indigo-400 to-blue-500 h-2 rounded-full skill-bar" style="width: 0%"></div>
			</div>
		</div>
	</div>
</div>

			</div>

			<!-- Right Content - Interactive Image -->
			<div class="lg:order-last">
				<div class="relative">
					<!-- Main Image Container -->
					<div class="relative overflow-hidden rounded-2xl shadow-2xl transform hover:scale-105 transition-transform duration-500">
						<div class="aspect-w-4 aspect-h-5 bg-gradient-to-br from-blue-500 via-purple-500 to-pink-500 p-8">
							<!-- Placeholder for profile image -->
							<div class="flex items-center justify-center text-white">
								<svg class="w-32 h-32" fill="currentColor" viewBox="0 0 24 24">
									<path d="M12 12c2.21 0 4-1.79 4-4s-1.79-4-4-4-4 1.79-4 4 1.79 4 4 4zm0 2c-2.67 0-8 1.34-8 4v2h16v-2c0-2.66-5.33-4-8-4z"/>
								</svg>
							</div>
						</div>
						
						<!-- Overlay Effects -->
						<div class="absolute inset-0 bg-gradient-to-t from-black/20 to-transparent"></div>
					</div>

					<!-- Floating Elements -->
					<div class="absolute -top-4 -right-4 w-20 h-20 bg-blue-500 rounded-full flex items-center justify-center text-white font-bold text-xl shadow-lg floating-element">
						2+
					</div>
					<div class="absolute -bottom-4 -left-4 w-16 h-16 bg-purple-500 rounded-full flex items-center justify-center text-white shadow-lg floating-element animation-delay-1000">
						<svg class="w-8 h-8" fill="currentColor" viewBox="0 0 24 24">
							<path d="M12 2l3.09 6.26L22 9l-5 4.87 1.18 6.88L12 17.77l-6.18 2.98L7 14.87 2 10l6.91-1.74L12 2z"/>
						</svg>
					</div>
					<div class="absolute top-1/2 -right-8 w-12 h-12 bg-green-500 rounded-full flex items-center justify-center text-white shadow-lg floating-element animation-delay-2000">
						<svg class="w-6 h-6" fill="currentColor" viewBox="0 0 24 24">
							<path d="M9 12l2 2 4-4m6 2a9 9 0 11-18 0 9 9 0 0118 0z"/>
						</svg>
					</div>

					<!-- Background Decoration -->
					<div class="absolute -z-10 top-8 left-8 w-full h-full bg-gradient-to-br from-blue-100 to-purple-100 dark:from-blue-900/20 dark:to-purple-900/20 rounded-2xl"></div>
				</div>
			</div>
		</div>
	</div>
</section>

<style>
	@keyframes float-gentle {
		0%, 100% { transform: translateY(0px); }
		50% { transform: translateY(-10px); }
	}
	.floating-element {
		animation: float-gentle 3s ease-in-out infinite;
	}
	.skill-bar {
		transition: width 2s ease-in-out;
	}
	/* Scroll-reveal Netflix-style fade/zoom utility classes */
	.sr-reveal {
		opacity: 0;
		transform: translateY(40px) scale(0.96);
		transition:
			opacity 0.7s cubic-bezier(0.4,0,0.2,1),
			transform 0.7s cubic-bezier(0.4,0,0.2,1);
		transition-delay: var(--sr-delay,0ms);
		will-change: opacity, transform;
	}
	.sr-reveal.sr-visible {
		opacity: 1;
		transform: translateY(0) scale(1);
	}
	.sr-card {
		opacity: 0;
		transform: translateY(40px) scale(0.96);
		transition:
			opacity 0.7s cubic-bezier(0.4,0,0.2,1),
			transform 0.7s cubic-bezier(0.4,0,0.2,1);
		transition-delay: var(--sr-delay,0ms);
		will-change: opacity, transform;
	}
	.sr-card.sr-visible {
		opacity: 1;
		transform: translateY(0) scale(1);
	}
	.sr-zoom {
		opacity: 0;
		transform: scale(0.98);
		transition:
			opacity 0.9s cubic-bezier(0.4,0,0.2,1),
			transform 0.9s cubic-bezier(0.4,0,0.2,1);
		will-change: opacity, transform;
	}
	.sr-zoom.sr-visible {
		opacity: 1;
		transform: scale(1);
	}
	@media (prefers-reduced-motion: reduce) {
		.sr-reveal, .sr-card, .sr-zoom {
			transition: none !important;
		}
	}
</style>

<script>
// Scroll-reveal Netflix-style animation engine
function scrollRevealInit() {
	const revealEls = document.querySelectorAll('.sr-reveal, .sr-card, .sr-zoom');
	const prefersReducedMotion = window.matchMedia('(prefers-reduced-motion: reduce)').matches;
	if (prefersReducedMotion) {
		revealEls.forEach(el => el.classList.add('sr-visible'));
		return;
	}
	const observer = new IntersectionObserver((entries) => {
		entries.forEach(entry => {
			if (entry.isIntersecting) {
				entry.target.classList.add('sr-visible');
			} else {
				entry.target.classList.remove('sr-visible');
			}
		});
	}, { threshold: 0.3 });
	revealEls.forEach(el => observer.observe(el));
}

// Counter animation (only when visible)
function animateCounters() {
	const counters = document.querySelectorAll('.sr-card .counter');
	counters.forEach(counterEl => {
		let started = false;
		const parent = counterEl.closest('.sr-card');
		const target = parseInt(parent.dataset.target);
		const increment = target / 50;
		let current = 0;
		const observer = new IntersectionObserver((entries) => {
			entries.forEach(entry => {
				if (entry.isIntersecting && !started) {
					started = true;
					const timer = setInterval(() => {
						current += increment;
						if (current >= target) {
							current = target;
							clearInterval(timer);
						}
						counterEl.textContent = Math.floor(current) + (target > 99 ? '+' : '');
					}, 40);
				}
				if (!entry.isIntersecting && started) {
					// Optionally reset counter if you want reverse animation
				}
			});
		}, { threshold: 0.7 });
		observer.observe(parent);
	});
}

// Skill bars animation (only when visible)
function animateSkillBars() {
	const skillItems = document.querySelectorAll('.skill-item');
	skillItems.forEach(item => {
		let animated = false;
		const skillBar = item.querySelector('.skill-bar');
		const skillPercentage = item.dataset.skill;
		const observer = new IntersectionObserver((entries) => {
			entries.forEach(entry => {
				if (entry.isIntersecting && !animated) {
					animated = true;
					setTimeout(() => {
						skillBar.style.width = skillPercentage + '%';
					}, 200);
				}
				if (!entry.isIntersecting && animated) {
					// Optionally reset bar if you want reverse animation
				}
			});
		}, { threshold: 0.5 });
		observer.observe(item);
	});
}

document.addEventListener('DOMContentLoaded', () => {
	scrollRevealInit();
	animateCounters();
	animateSkillBars();
});
</script>
